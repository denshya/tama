---
id: 31
assignee: sanjay14073
~github: pinely-international/tama#74
---

Add a Vite-plugin that compresses JSX, so that it doesn't take that much space as currently. There are different approaches to this, it can shorthand element names, attributes (most common `className`) to just a few letters (e.g. `cn` <- `className`).
